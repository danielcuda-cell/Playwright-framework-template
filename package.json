{
  "name": "TEST1",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "playwright test",
    "record-test": "playwright codegen",
    "show-report": "playwright show-report",
    "test-and-report": "playwright test && playwright show-report",
    "allure:report": "powershell -Command \"if (Test-Path allure-report/history) { Copy-Item allure-report/history allure-results -Recurse -Force }; npx allure generate allure-results -o allure-report --clean\"",
    "allure:open": "npx allure open allure-report",
    "generate:test": "tsx scripts/generateTest.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "@types/node": "^24.10.1",
    "ajv": "^8.17.1",
    "allure-commandline": "^2.36.0",
    "allure-playwright": "^3.4.5"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.99.0",
    "ajv-formats": "^3.0.1",
    "dotenv": "^17.2.3",
    "mailslurp-client": "^17.2.0"
  }
}
